<?php

// Sem provider de app/ — todo o código do projeto vive nos módulos (Modules/*), cujos service
// providers são descobertos automaticamente pelo nwidart/laravel-modules (modules_statuses.json
// + module.json de cada um), não registrados aqui.

return [];
